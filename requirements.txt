# Ergeon internal PyPI server (for internal packages)
--extra-index-url=https://erg-bot:q8zgdmot3@pypi.ergeon.in/simple/

# Core dependencies for parcel-ai-json package
ultralytics>=8.0.0      # YOLOv8
pillow>=9.0.0           # Image processing
numpy>=1.20.0
torch>=2.0.0            # PyTorch for YOLO
torchvision>=0.15.0
pyproj>=3.0.0           # Geodesic coordinate transformations

# Tree detection with DeepForest (individual tree crowns) and detectree (coverage polygons)
deepforest>=2.0.0
detectree==0.8.0      # Tree coverage detection (pixel-level classification)

# SAM - Segment Anything Model for general-purpose segmentation
git+https://github.com/facebookresearch/segment-anything.git  # Zero-shot segmentation
opencv-python>=4.8.0  # Required for SAM polygon extraction from masks

# GroundingDINO - Open-vocabulary object detection with text prompts
git+https://github.com/IDEA-Research/GroundingDINO.git  # Text-prompted object detection
transformers>=4.30.0  # Required for GroundingDINO text encoding
supervision>=0.16.0   # Required for GroundingDINO post-processing

# API dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Visualization (folium maps, matplotlib)
folium>=0.14.0
matplotlib>=3.5.0

# Development dependencies (install with: pip install -e ".[dev]")
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=23.0.0
# flake8>=6.0.0
