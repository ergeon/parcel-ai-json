# Ergeon internal PyPI server (for internal packages)
--extra-index-url=https://erg-bot:q8zgdmot3@pypi.ergeon.in/simple/

# Core dependencies for parcel-ai-json package
ultralytics>=8.0.0      # YOLOv8
pillow>=9.0.0           # Image processing
numpy>=1.20.0
torch>=2.0.0            # PyTorch for YOLO
torchvision>=0.15.0
pyproj>=3.0.0           # Geodesic coordinate transformations

# Tree detection with DeepForest (individual tree crowns) and detectree (coverage polygons)
deepforest>=2.0.0
detectree==0.8.0      # Tree coverage detection (pixel-level classification)

# SAM - Segment Anything Model for general-purpose segmentation (original)
git+https://github.com/facebookresearch/segment-anything.git  # Zero-shot segmentation
opencv-python>=4.8.0  # Required for SAM polygon extraction from masks

# SAM3 - Segment Anything Model 3 for open-vocabulary segmentation
# Install SAM3 from local models directory: pip install -e models/sam3
# SAM3 dependencies (required for SAM3 to work):
timm>=1.0.17          # Vision transformer models for SAM3
ftfy==6.1.1           # Text encoding fixes for SAM3
regex                 # Text processing for SAM3
iopath>=0.1.10        # File I/O utilities
typing_extensions     # Type hints for older Python versions
huggingface_hub>=0.20.0  # HuggingFace model hub (for SAM3 model download)
einops>=0.8.0         # Tensor operations for SAM3
python-dotenv>=1.0.0  # Environment variable management
triton                # GPU kernel acceleration for SAM3

# GroundingDINO dependencies (install BEFORE GroundingDINO)
transformers>=4.30.0  # Required for GroundingDINO text encoding
supervision>=0.16.0   # Required for GroundingDINO post-processing
addict>=2.4.0         # Required by GroundingDINO
yapf>=0.40.0          # Required by GroundingDINO

# GroundingDINO - Open-vocabulary object detection with text prompts
git+https://github.com/IDEA-Research/GroundingDINO.git  # Text-prompted object detection

# API dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Claude API for intelligent agents (optional - agents fallback to defaults if not installed)
anthropic>=0.40.0

# Visualization (folium maps, matplotlib)
folium>=0.14.0
matplotlib>=3.5.0

# Development dependencies (install with: pip install -e ".[dev]")
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=23.0.0
# flake8>=6.0.0
